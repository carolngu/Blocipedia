<h1>New Wiki</h1>

<div class="row">
  <div class="col-md-4">
    <p>Rules for New Wiki:</p>
    <ul>
      <li>Make sure Wiki is true.</li>
      <li>Anything offensive is frowned upon. (I will get you.)</li>
      <li>Make the wifi fun to read.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @wiki do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "What's your Wiki title?" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Describe your Wiki here." %>
      </div>
      <div class="form-group">
        <%= f.label :private, class: 'checkbox' do %>
          <%= f.check_box :private %> Do you want your Wiki to be private?
        <% end %>
      </div>
      <%= f.submit "Save", class: "btn btn-success" %>
    <% end %>
  </div>
</div>
